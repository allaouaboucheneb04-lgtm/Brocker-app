rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function signedIn() {
      return request.auth != null;
    }

    // Règles de développement v23.1 : stables pendant les tests.
    match /{document=**} {
      allow read, write: if signedIn();
    }
  }
}
